const CHARTS = require('./charts');
const {
  FORMS,
  COMPUTED_FORMS
} = require('./forms');
const REPORTS = require('./reports');

module.exports = {
  CHARTS,
  COMPUTED_FORMS,
  FORMS,
  REPORTS
};
